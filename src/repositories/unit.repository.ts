import { query } from '../config/database';
export async function findUnits(companyId:string,buildingId:string){return (await query(`SELECT u.* FROM rental_units u JOIN rental_buildings b ON b.id=u.building_id JOIN rental_properties p ON p.id=b.property_id WHERE p.company_id=$1 AND u.building_id=$2 ORDER BY u.unit_number`,[companyId,buildingId])).rows;}
export async function findUnitById(companyId:string,id:string){return (await query(`SELECT u.* FROM rental_units u JOIN rental_buildings b ON b.id=u.building_id JOIN rental_properties p ON p.id=b.property_id WHERE p.company_id=$1 AND u.id=$2`,[companyId,id])).rows[0]??null;}
export async function createUnit(buildingId:string,d:any){return (await query(`INSERT INTO rental_units(building_id,unit_type_id,unit_number,floor,monthly_rent,deposit_amount,status,description) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[buildingId,d.unitTypeId??null,d.unitNumber,d.floor??null,d.monthlyRent,d.depositAmount??0,d.status??'VACANT',d.description??null])).rows[0];}
export async function updateUnit(companyId:string,id:string,d:any){return (await query(`UPDATE rental_units u SET unit_type_id=COALESCE($3,u.unit_type_id),unit_number=COALESCE($4,u.unit_number),floor=COALESCE($5,u.floor),monthly_rent=COALESCE($6,u.monthly_rent),deposit_amount=COALESCE($7,u.deposit_amount),status=COALESCE($8,u.status),description=COALESCE($9,u.description),updated_at=NOW() FROM rental_buildings b JOIN rental_properties p ON p.id=b.property_id WHERE u.id=$2 AND u.building_id=b.id AND p.company_id=$1 RETURNING u.*`,[companyId,id,d.unitTypeId,d.unitNumber,d.floor,d.monthlyRent,d.depositAmount,d.status,d.description])).rows[0]??null;}
export async function deleteUnit(companyId:string,id:string){return (await query(`DELETE FROM rental_units u USING rental_buildings b,rental_properties p WHERE u.id=$2 AND u.building_id=b.id AND b.property_id=p.id AND p.company_id=$1 RETURNING u.id`,[companyId,id])).rows[0]??null;}
